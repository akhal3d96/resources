# certbot

In case of custome web server configuration I find DNS challenges more convinent and easier to work with.

```
certbot certonly --manual --preferred-challenges=dns --email email@example.com  -d subdomain.example.com
```